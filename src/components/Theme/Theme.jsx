import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#F69D55', // Change this to your desired primary color
    },
  },
});

export default Theme;
